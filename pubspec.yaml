name: numbers_bloc
description: "Numbers App by Abbos Donaboyev"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  flutter_bloc: ^9.1.1
  toastification: ^3.0.3
  freezed_annotation: ^3.0.0
  connectivity_plus: ^6.1.4
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.3
  freezed: ^3.0.6

flutter:
  uses-material-design: true
